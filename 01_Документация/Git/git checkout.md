Используется для смены рабочей директории под новую ветку, делая её активной веткой.  


- `git checkout <name>` - переключается на ветку.
- `git checkout -b <name>` - создает новую ветку и переключается на неё.