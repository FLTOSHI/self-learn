Команда для добавления, просмотра и конфигурации настроек, которые определяют поведение и внешний вид окружения Git. Часто используются:

`git config --global user.name`
`git config --global user.email`